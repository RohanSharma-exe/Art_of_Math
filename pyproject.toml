[project]
name = "generative-art-engine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ROHAN", email = "rohan.sharma1234987650@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.2",
    "pillow>=12.3.0",
]

[project.scripts]
generative-art-engine = "generative_art_engine:main"

[build-system]
requires = ["uv_build>=0.11.24,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.2",
]
